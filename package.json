{
  "name": "anigo-scraper-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "concurrently \"python api/index.py\" \"cd frontend && npm run dev\"",
    "install-all": "pip install -r api/requirements.txt && cd frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
